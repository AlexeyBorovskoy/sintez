# Анализ проблемы с желтым миганием на основе документации

**Дата:** 2026-02-03  
**Источник:** `/home/alexey/shared_vm/spectr_utmc/spectr_utmc/docs/protocol.txt`

---

## 🔍 Критическая информация из документации

### Описание SetAF из protocol.txt:

```
SetAF : // включение  состояния ЖМ

    """1.3.6.1.4.1.13267.3.2.4.2.1.20

    Where a condition '1' exists for a minimum of 10 seconds,

    the signals shall be set to flashing amber during

    a nominated stage provided that all minimum running periods have expired.

    Where a condition '0' exists for a minimum of 10 seconds,

    the signals shall switch on in accordance with the start up sequence.

    This facility is only specified for export (non-UK) applications.

    INTEGER: 1"""
```

### ⚠️ Ключевые требования:

1. **Условие '1' должно существовать минимум 10 секунд**
   - Это означает, что FF=1 должен быть установлен и **удерживаться** минимум 10 секунд
   - Недостаточно просто установить FF=1 один раз

2. **Сигналы будут установлены в режим желтого мигания во время назначенной фазы**
   - Контроллер переключится в ЖМ не сразу, а во время определенной фазы
   - Это может произойти не мгновенно

3. **При условии, что все минимальные периоды работы истекли**
   - Контроллер должен завершить текущую фазу
   - Минимальные периоды работы фаз должны истечь

---

## 🔑 ВАЖНО: Требование передачи SCN

Из protocol.txt, строка 155:

```
OID с именами 'SysDescr', 'Uptime', 'GetVersion', 'GetPhase', 'GetError', 
'GetWarnings', 'GetRCP', 'GetAF', 'SetPhase', 'StageDemandBits', 
'SetPhaseTrue', 'SetAF', 'SetOFF', 'GetOFF' работают с передачей SCN
```

**SetAF требует передачи SCN!**

---

## ❌ Проблема в нашем коде

### Что мы делали:

1. Устанавливали FF=1 **без передачи SCN**
2. Не удерживали FF=1 минимум 10 секунд
3. Не учитывали необходимость завершения минимальных периодов работы фаз

### Что нужно делать:

1. **Передавать SCN в SET запросе для SetAF**
   - Согласно документации, SetAF работает с передачей SCN
   - SCN должен быть в формате `COX`, где CO - обязательные символы, X - число от 1 до 9999

2. **Удерживать FF=1 минимум 10 секунд**
   - Не просто установить один раз, а поддерживать значение 1 в течение минимум 10 секунд

3. **Дождаться завершения текущей фазы**
   - Контроллер переключится в ЖМ во время назначенной фазы
   - После того, как все минимальные периоды работы истекли

---

## 📋 Информация о SCN из protocol.txt

```
Для управления контроллером по протоколу UG405 необходимо выполнить следующие настройки в web интерфейсе:

Во вкладке «Управление контроллером» выбрать из списка протоколов UG405 и задать SCN.

Обязательным требованием является следующий формат: SCN = COX, где "CO" обязательные символы латинскими буквами, а X - число от 1 до 9999;
```

**SCN из config.json:** `CO1111` (уже настроен в веб-интерфейсе)

---

## 🔧 Как передается SCN в SNMP запросах?

Согласно документации и анализу кода:
- SCN передается как часть индекса в OID для табличных объектов
- Для SetAF (UTC_CONTROL_FF) OID: `1.3.6.1.4.1.13267.3.2.4.2.1.20`
- Но это скалярный объект, не табличный

**Вопрос:** Как именно передается SCN для SetAF?

Возможные варианты:
1. SCN передается как часть OID (например, `1.3.6.1.4.1.13267.3.2.4.2.1.20.SCN`)
2. SCN передается в значении varbind
3. SCN передается в community string
4. SCN передается в другом формате

---

## 📝 Рекомендации для исправления

### 1. Проверить передачу SCN

Нужно выяснить, как именно передается SCN для SetAF. Возможные варианты:

**Вариант A: SCN в OID (для табличных объектов)**
- Для табличных объектов SCN может быть частью индекса
- Но SetAF - это скалярный объект

**Вариант B: SCN в значении**
- Возможно, SCN должен быть частью значения varbind
- Но документация говорит об INTEGER: 1

**Вариант C: SCN не требуется для SetAF**
- Возможно, документация неточна
- Или SCN требуется только для GET операций

### 2. Удерживать FF=1 минимум 10 секунд

Вместо однократной установки FF=1, нужно:
- Установить FF=1
- Подождать минимум 10 секунд
- Проверить результат

### 3. Проверить текущую фазу

Перед установкой FF=1:
- Проверить текущую фазу
- Убедиться, что минимальные периоды работы могут истечь
- Возможно, нужно установить определенную фазу перед включением ЖМ

---

## 🔍 Дополнительная информация из руководства пользователя

Из руководства пользователя (PDF):

> "Если текущее значение тока оказывается ниже порога для красного сигнала, это означает что в группе не осталось ни одной работающей красной лампы. Это потенциально опасная для участников дорожного движения ситуация, при возникновении которой контроллер **автоматически переходит в режим «Желтое мигание»**."

Это означает, что контроллер может автоматически переходить в режим ЖМ при определенных условиях. Возможно, для ручного включения через SNMP нужны дополнительные условия.

---

## ✅ План действий

1. **Проверить передачу SCN:**
   - Изучить, как исходный Node.js код передает SCN для SetAF
   - Проверить, требуется ли SCN для SetAF или только для GET операций

2. **Изменить логику включения ЖМ:**
   - Установить FF=1
   - Удерживать FF=1 минимум 10 секунд (периодически проверять/устанавливать)
   - Дождаться переключения в ЖМ

3. **Проверить условия:**
   - Убедиться, что контроллер в режиме UTC Control (3)
   - Проверить текущую фазу
   - Убедиться, что минимальные периоды работы могут истечь

4. **Протестировать с учетом всех условий:**
   - Установить режим UTC Control (3)
   - Установить FF=1 с передачей SCN (если требуется)
   - Удерживать FF=1 минимум 10 секунд
   - Проверить результат через GetAF

---

## 📌 Выводы

1. **SetAF требует передачи SCN** (согласно документации)
2. **FF=1 должен удерживаться минимум 10 секунд** (не однократная установка)
3. **Контроллер переключится в ЖМ во время назначенной фазы** (не мгновенно)
4. **Все минимальные периоды работы фаз должны истечь** (перед переключением)

**Основная проблема:** Мы не передаем SCN и не удерживаем FF=1 достаточно долго!
