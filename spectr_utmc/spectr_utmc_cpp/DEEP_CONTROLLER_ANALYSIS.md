# Глубокий анализ контроллера SINTEZ UTMC

**Дата анализа:** 2026-02-04  
**Источник:** Полный снэпшот контроллера (192.168.75.150)

---

## 🔍 КРИТИЧЕСКИ ВАЖНЫЕ НАХОДКИ

### 1. Режим работы контроллера

**Из конфигурации `/home/voicelink/rtc/resident/config`:**
```json
{
    "mode": "local",           // ⚠️ КОНТРОЛЛЕР В ЛОКАЛЬНОМ РЕЖИМЕ!
    "remote_onoff": false,     // ⚠️ УДАЛЁННОЕ УПРАВЛЕНИЕ ВЫКЛЮЧЕНО!
    "snmp_proto": "UTMC",
    "snmp_scn": "",            // ✅ SCN пустой (подтверждает наш анализ)
    "deny_rcp": false
}
```

**Вывод:** Контроллер работает в **локальном режиме**, а не в режиме удалённого управления (UTC Control). Это объясняет, почему команды SET_YF не активируют мигание!

### 2. Защита от ошибок жёлтого мигания

**Из логов `/home/voicelink/rtc/resident/info.log`:**
```
0x1042;;Deny the remote controll in case of amber flash error: {0}.;;True
```

**Перевод:** "Запретить удалённое управление в случае ошибки жёлтого мигания: True"

**Вывод:** Контроллер имеет **защитный механизм**, который **отключает удалённое управление** при ошибках жёлтого мигания. Это может быть причиной того, что команды SET_YF не работают!

### 3. Архитектура контроллера

**Ключевые процессы:**
1. **resident** (PID 357, 88.8% CPU) - основной процесс контроллера светофора
2. **snmp_agent** (PID 463, 82.8% CPU) - кастомный SNMP агент, обрабатывающий SNMP запросы
3. **spectr_utmc.js** (PID 458) - мост между UTMC и Spectr-ITS

**Важно:** 
- `spectr_utmc.js` работает с адресом `127.0.0.1` (локально)
- SNMP запросы идут к локальному `snmp_agent`
- `snmp_agent` обрабатывает запросы и передаёт их процессу `resident`

### 4. Конфигурация фаз

**Из конфигурации:**
```json
{
    "programm": {
        "phases": {
            "specialPhases": [1, 2, 3, 4],  // Специальные фазы
            "crossTime": {
                "1": [2, [10, 3, 5, 20], 0, 3, 0]  // Минимальное время: 10 сек!
            }
        },
        "plans": {
            "1": {
                "actions": [
                    {
                        "min_time": 5,      // Минимальное время фазы
                        "num_phase": 1,
                        "safe_time": 3,     // Безопасное время
                        "max_time": 20,     // Максимальное время
                        "fix_time": 10      // Фиксированное время
                    }
                ]
            }
        }
    }
}
```

**Вывод:** 
- Минимальное время работы фазы: **5-10 секунд**
- Безопасное время: **3 секунды**
- Это соответствует требованиям документации о "minimum running periods"

---

## 🎯 ПРИЧИНЫ ПРОБЛЕМЫ С ЖЁЛТЫМ МИГАНИЕМ

### Причина 1: Контроллер в локальном режиме

**Проблема:**
- Контроллер работает в режиме `"mode": "local"`
- Удалённое управление отключено: `"remote_onoff": false`
- Команды SET_YF требуют режим UTC Control (3), но контроллер в локальном режиме (1)

**Решение:**
- Перед активацией мигания необходимо перевести контроллер в режим удалённого управления
- Установить `operationMode = 3` (UTC Control)
- Дождаться подтверждения перехода в режим

### Причина 2: Защита от ошибок мигания

**Проблема:**
- Контроллер имеет защиту: "Deny the remote controll in case of amber flash error: True"
- Если была ошибка мигания, удалённое управление автоматически отключается
- Команды SET_YF могут игнорироваться из-за этой защиты

**Решение:**
- Проверить логи на наличие ошибок мигания
- Если есть ошибки - исправить их перед активацией мигания
- Возможно, требуется сброс защиты или ручное включение удалённого управления

### Причина 3: Минимальные периоды работы фаз

**Проблема:**
- Контроллер требует истечения минимальных периодов работы фаз (5-10 секунд)
- Команда SET_YF может игнорироваться, если фаза работает меньше минимального времени
- Документация требует "nominated stage" и истёкшие минимальные периоды

**Решение:**
- Перед активацией мигания проверить текущую фазу
- Дождаться истечения минимального периода работы фазы (минимум 5-10 секунд)
- Активировать мигание только в "nominated stage" (специальные фазы: 1, 2, 3, 4)

---

## 💡 ПРЕДЛАГАЕМОЕ РЕШЕНИЕ

### Этап 1: Проверка и подготовка контроллера

1. **Проверить текущий режим работы:**
   ```bash
   snmpget -v2c -c UTMC 192.168.75.150 1.3.6.1.4.1.13267.3.2.4.1
   ```

2. **Проверить наличие ошибок мигания:**
   - Изучить логи `resident/error.log`
   - Проверить статус защиты от ошибок мигания

3. **Проверить текущую фазу и время её работы:**
   ```bash
   snmpget -v2c -c UTMC 192.168.75.150 1.3.6.1.4.1.13267.3.2.5.1.1.3  # Текущая фаза
   snmpget -v2c -c UTMC 192.168.75.150 1.3.6.1.4.1.13267.3.2.5.1.1.5  # Счётчик фазы
   snmpget -v2c -c UTMC 192.168.75.150 1.3.6.1.4.1.13267.3.2.5.1.1.4  # Длительность фазы
   ```

### Этап 2: Перевод в режим UTC Control

**ВАЖНО:** Контроллер должен быть в режиме UTC Control (3) для активации мигания!

1. **Перевести контроллер в режим UTC Control:**
   ```bash
   snmpset -v2c -c UTMC 192.168.75.150 \
       1.3.6.1.4.1.13267.3.2.4.1 i 3
   ```

2. **Подождать стабилизации режима (2-3 секунды)**

3. **Проверить успешность перевода:**
   ```bash
   snmpget -v2c -c UTMC 192.168.75.150 1.3.6.1.4.1.13267.3.2.4.1
   ```

### Этап 3: Подготовка фазы

1. **Проверить текущую фазу:**
   - Если фаза не является специальной (1, 2, 3, 4) - установить специальную фазу
   - Если фаза специальная - проверить время её работы

2. **Дождаться истечения минимального периода:**
   - Минимальное время: 5-10 секунд
   - Безопасное время: 3 секунды
   - **Итого: минимум 8-13 секунд работы фазы**

3. **Установить фазу (если необходимо):**
   ```bash
   snmpset -v2c -c UTMC 192.168.75.150 \
       1.3.6.1.4.1.13267.3.2.4.1 i 3 \
       1.3.6.1.4.1.13267.3.2.4.2.1.5 x 01  # Фаза 1 (битовая маска 0x01)
   ```

### Этап 4: Активация жёлтого мигания

**Только после выполнения всех предыдущих этапов!**

1. **Активировать мигание:**
   ```bash
   snmpset -v2c -c UTMC 192.168.75.150 \
       1.3.6.1.4.1.13267.3.2.4.1 i 3 \
       1.3.6.1.4.1.13267.3.2.4.2.1.20 i 1
   ```

2. **Удерживать команду (как в оригинальном решении):**
   - Отправлять команду каждые 2 секунды в течение 60 секунд
   - Это обеспечит выполнение требования "команда активна минимум 10 секунд"

3. **Мониторить активацию:**
   ```bash
   snmpget -v2c -c UTMC 192.168.75.150 1.3.6.1.4.1.13267.3.2.5.1.1.36  # utcReplyFR
   ```

---

## 🔧 ОБНОВЛЁННАЯ ЛОГИКА ДЛЯ C++ КОДА

### Алгоритм активации SET_YF:

```cpp
SpectrError SpectrObject::setYF(const std::string& requestId) {
    // ШАГ 1: Проверка текущего режима работы
    uint8_t currentMode = getCurrentOperationMode();
    
    if (currentMode != 3) {
        // ШАГ 2: Перевод в режим UTC Control
        if (!switchToUTCMode()) {
            return SpectrError::NOT_EXEC_5;
        }
        // Пауза для стабилизации
        std::this_thread::sleep_for(std::chrono::seconds(3));
    }
    
    // ШАГ 3: Проверка текущей фазы
    PhaseInfo phaseInfo = getCurrentPhaseInfo();
    
    // ШАГ 4: Проверка минимального периода работы фазы
    if (phaseInfo.counter < phaseInfo.minTime) {
        // Дождаться истечения минимального периода
        int waitTime = phaseInfo.minTime - phaseInfo.counter + 3; // +3 сек безопасности
        std::this_thread::sleep_for(std::chrono::seconds(waitTime));
    }
    
    // ШАГ 5: Проверка, является ли фаза специальной (nominated stage)
    if (!isSpecialPhase(phaseInfo.phase)) {
        // Установить специальную фазу (например, фазу 1)
        if (!setPhase(requestId, 1)) {
            return SpectrError::NOT_EXEC_5;
        }
        // Дождаться активации фазы и истечения минимального периода
        std::this_thread::sleep_for(std::chrono::seconds(13)); // 10 сек минимум + 3 сек безопасности
    }
    
    // ШАГ 6: Активация мигания с удержанием команды
    startYFHold();  // Удержание команды 60 секунд
    
    return SpectrError::OK;
}
```

---

## 🧪 СОЗДАНИЕ ВИРТУАЛЬНОГО КОНТРОЛЛЕРА

### Цель:
Создать виртуальный контроллер на основе снэпшота для тестирования без риска для реального оборудования.

### Компоненты виртуального контроллера:

1. **SNMP Agent (эмулятор):**
   - Обработка SNMP GET/SET запросов
   - Эмуляция OID контроллера
   - Логирование всех операций

2. **Resident Emulator:**
   - Эмуляция логики контроллера светофора
   - Обработка режимов работы (local/remote)
   - Эмуляция фаз и их временных параметров
   - Эмуляция защиты от ошибок мигания

3. **Конфигурация:**
   - Использование реальной конфигурации из снэпшота
   - Возможность изменения параметров для тестирования

### Преимущества:
- Безопасное тестирование без риска для реального оборудования
- Возможность тестирования различных сценариев
- Отладка логики активации мигания
- Воспроизведение проблем для анализа

---

## 📋 ПЛАН ДЕЙСТВИЙ

### Немедленные действия:

1. **Обновить C++ код:**
   - Добавить проверку режима работы перед SET_YF
   - Добавить проверку минимальных периодов работы фаз
   - Добавить проверку специальных фаз
   - Улучшить логику перевода в режим UTC Control

2. **Создать тестовый скрипт:**
   - Проверка режима работы
   - Проверка текущей фазы и времени её работы
   - Перевод в режим UTC Control
   - Подготовка фазы (если необходимо)
   - Активация мигания с удержанием

3. **Протестировать на реальном контроллере:**
   - Выполнить все этапы последовательно
   - Логировать все операции
   - Проверить визуальную активацию мигания

### Долгосрочные действия:

1. **Создать виртуальный контроллер:**
   - SNMP Agent эмулятор
   - Resident эмулятор
   - Веб-интерфейс для мониторинга

2. **Документировать протокол:**
   - Полное описание всех этапов активации мигания
   - Требования к режимам работы
   - Требования к фазам и временным параметрам

---

## ✅ ВЫВОДЫ

1. **Контроллер работает в локальном режиме** - это основная причина проблемы
2. **Есть защита от ошибок мигания** - может блокировать удалённое управление
3. **Требуются минимальные периоды работы фаз** - 5-10 секунд минимум
4. **Требуются специальные фазы** - фазы 1, 2, 3, 4 являются "nominated stages"
5. **Логика оригинального Node.js кода простая** - но она работает только при правильных условиях

**Решение:** Обновить C++ код для выполнения всех необходимых проверок и подготовительных этапов перед активацией мигания.
