# 🚀 НАЧНИТЕ ОТСЮДА

## Проект готов к передаче на сервер

### 📦 Что передать

**Архив:** `spectr_utmc_cpp.tar.gz` (21 KB)

Или всю папку `spectr_utmc_cpp/`

### ⚡ Быстрая замена (3 команды на сервере)

```bash
# 1. Остановить старую версию
sudo systemctl stop spectr_utmc

# 2. Распаковать и собрать
tar -xzf spectr_utmc_cpp.tar.gz && cd spectr_utmc_cpp && ./build.sh

# 3. Установить и запустить (см. REPLACE.md)
sudo cp build/spectr_utmc_cpp /usr/local/bin/
sudo cp config.json /etc/spectr_utmc/
sudo cp spectr_utmc.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable spectr_utmc.service
sudo systemctl start spectr_utmc.service
```

### 📖 Документация

1. **REPLACE.md** - ⭐ **ГЛАВНАЯ ИНСТРУКЦИЯ** по замене версии
2. **QUICKSTART.md** - Быстрый старт
3. **DEPLOY.md** - Детальное развертывание
4. **BUILD.md** - Инструкция по сборке
5. **STATUS.md** - Статус проекта

### ✅ Важно

- ✅ Используются **ТЕ ЖЕ** настройки что у оригинала
- ✅ Порты **НЕ МЕНЯЮТСЯ**: 10162 (SNMP), 3000 (ITS)
- ✅ Используйте **ТОТ ЖЕ** config.json
- ✅ **ОБЯЗАТЕЛЬНО** остановите старую версию перед запуском

### 🔍 Проверка после запуска

```bash
sudo systemctl status spectr_utmc.service
sudo journalctl -u spectr_utmc.service -f
```

### 📞 Если что-то пошло не так

См. раздел "Устранение проблем" в **REPLACE.md**

---

**Начните с файла REPLACE.md для детальной инструкции!**
