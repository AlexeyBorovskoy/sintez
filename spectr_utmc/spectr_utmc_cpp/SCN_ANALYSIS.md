# Анализ передачи SCN в SNMP запросах

**Дата:** 2026-02-03  
**Источник:** Документация (Excel файл `Sintez_snmp_protocols.xlsx` и MIB)

---

## 🔑 КРИТИЧЕСКОЕ ОТКРЫТИЕ

### Из Excel файла `Sintez_snmp_protocols.xlsx`:

**SetAF (желтое мигание):**
- OID: `1.3.6.1.4.1.13267.3.2.4.2.1.20 + SCN`
- Описание: "Установить состояние ЖМ: 0 - ЖМ выключить, 1 - ЖМ включить"

**SetPhase (установка фазы):**
- OID: `1.3.6.1.4.1.13267.3.2.4.2.1.5 + SCN`
- Описание: "Установка фазы: set HEX-STRING: 2**(num_phase-1)"

**SetPhaseTrue (разрешение установки фазы):**
- OID: `1.3.6.1.4.1.13267.3.2.4.2.1.15 + SCN`
- Описание: "Получение и установка разрешающего бита на выполнения всех команд где oid = oid + SCN"

---

## 📋 Из MIB файла `UTMC-UTMCFULLUTCTYPE2-MIB`:

### Структура таблицы utcControlTable:

```
utcControlTable OBJECT-TYPE
    SYNTAX SEQUENCE OF UtcControlEntry
    INDEX { utcControlTimeStamp, utcControlSiteID }
```

**Это ТАБЛИЦА с двумя индексами:**
1. **utcControlTimeStamp** (UTCType2TimeStamp) - секунды дня
2. **utcControlSiteID** (UTCType2SiteID) - SCN

### Формат индексов:

**utcControlTimeStamp:**
- Тип: INTEGER (1..86402)
- Описание: "Seconds of Day +2. Universal Time Co-ordinated Time"
- **Важно:** "A timestamp value of 1 should be taken to mean 'NOW'"

**utcControlSiteID:**
- Тип: OCTET STRING (SIZE (1..16))
- Описание: "Identifies which, of possibly several, equipment at this site the object should be applied to"
- Формат: "Free Text ASCII String. Typically this could be an SCN, IP address, or a number."

---

## 🔧 Как формируется OID с SCN

### Для табличных объектов (utcControlTable):

**Формат OID:** `BASE_OID.timestamp.siteID`

**Примеры:**
- SetPhase: `1.3.6.1.4.1.13267.3.2.4.2.1.5.1.CO11111`
  - `1` = timestamp (NOW)
  - `CO11111` = SCN (SiteID)

- SetAF: `1.3.6.1.4.1.13267.3.2.4.2.1.20.1.CO11111`
  - `1` = timestamp (NOW)
  - `CO11111` = SCN (SiteID)

### Преобразование SCN в числовой формат:

SCN передается как строка ASCII, но в SNMP OID все компоненты должны быть числами!

**Проблема:** SCN = "CO11111" содержит буквы "CO", которые нельзя напрямую использовать в OID.

**Возможные решения:**

1. **SCN передается как ASCII коды символов:**
   - C = 67 (ASCII)
   - O = 79 (ASCII)
   - 1 = 49 (ASCII)
   - ...
   - OID: `1.3.6.1.4.1.13267.3.2.4.2.1.5.1.67.79.49.49.49.49.49`

2. **SCN передается как одно число (если только цифры):**
   - Но "CO11111" содержит буквы!

3. **SCN передается в другом формате:**
   - Возможно, нужно преобразовать "CO11111" в число
   - Или использовать только числовую часть "11111"

---

## 🔍 Анализ исходного Node.js кода

Нужно проверить, как исходный Node.js код формирует OID с SCN для SetPhase и SetAF.

---

## ✅ Рекомендации для исправления

### 1. Проверить формат передачи SCN

Нужно выяснить:
- Как именно передается SCN в OID
- Преобразуется ли "CO11111" в числа
- Или используется другой формат

### 2. Обновить код для передачи SCN

Если SCN передается как ASCII коды:
```cpp
// Преобразование SCN в числовые компоненты OID
std::string scn = "CO11111";  // из config
std::string oidWithSCN = baseOID + ".1";  // timestamp = 1 (NOW)
for (char c : scn) {
    oidWithSCN += "." + std::to_string(static_cast<unsigned char>(c));
}
```

### 3. Протестировать с правильным форматом

После исправления протестировать:
- SetPhase с SCN
- SetAF с SCN
- Визуальное подтверждение работы

---

## 📝 Выводы

1. **SetAF и SetPhase ТРЕБУЮТ передачи SCN** (подтверждено документацией)
2. **SCN передается как часть OID** (табличные объекты)
3. **Timestamp = 1 означает "NOW"** (немедленное выполнение)
4. **Нужно выяснить формат преобразования SCN в числовые компоненты OID**

---

## ⚠️ Текущая проблема

**Мы НЕ передаем SCN в SET запросах!**

Это объясняет, почему:
- SetPhase выполняется, но может работать некорректно
- SetAF не включает желтое мигание

**Решение:** Добавить передачу SCN в OID для всех операций, которые его требуют.
