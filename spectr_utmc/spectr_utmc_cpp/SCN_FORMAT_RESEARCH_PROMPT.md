# Промпт для поиска информации по форматам SCN

## 🎯 Цель исследования

Определить правильный формат передачи SCN (Site Control Number) в SNMP запросах для контроллера Синтез по протоколу UG405.

---

## 📋 Что нужно предоставить для эффективной работы

### 1. **Исходный Node.js код (если доступен)**

**Что искать:**
- Файл `spectr_utmc.js` или аналогичный
- Функции, которые выполняют SNMP SET операции
- Как формируется OID для SetPhase, SetAF, SetOFF
- Как передается SCN в запросах

**Примеры вопросов:**
- Как формируется OID для `utcControlFn` (SetPhase)?
- Как передается `siteId` или `SCN` в SNMP запросах?
- Есть ли преобразование SCN перед передачей?

**Команды для поиска:**
```bash
# Поиск в коде
grep -r "utcControlFn\|siteId\|SCN\|site" /path/to/nodejs/code
grep -r "1.3.6.1.4.1.13267.3.2.4.2.1.5" /path/to/nodejs/code
```

---

### 2. **Примеры реальных SNMP запросов (если есть логи)**

**Что искать:**
- Логи работы Node.js приложения
- Примеры успешных SNMP SET запросов
- Примеры успешных SNMP GET запросов
- Wireshark/tshark дампы SNMP трафика

**Что нужно:**
- Полный OID из успешного запроса
- Значение SCN, которое использовалось
- Формат OID (с точками, с индексами, и т.д.)

**Пример:**
```
Успешный SET Phase запрос:
OID: 1.3.6.1.4.1.13267.3.2.4.2.1.5.???
SCN: CO11111
```

---

### 3. **Документация контроллера Синтез**

**Что искать:**
- Руководство по настройке протокола UG405
- Примеры SNMP запросов с SCN
- Описание формата передачи SCN
- Техническая документация по SNMP интерфейсу

**Где искать:**
- Веб-интерфейс контроллера (раздел помощи)
- PDF документация от производителя
- Техническая поддержка производителя

**Вопросы для документации:**
- Как именно передается SCN в OID?
- Нужно ли преобразование SCN перед передачей?
- Есть ли примеры успешных запросов?

---

### 4. **Примеры работы с другими контроллерами (если есть)**

**Что искать:**
- Другие проекты интеграции с контроллерами Синтез
- Примеры кода от других разработчиков
- Форумы/сообщества по контроллерам Синтез

**Что нужно:**
- Примеры кода, который работает
- Формат OID, который используется
- Как передается SCN

---

### 5. **Тестирование с разными форматами SCN**

**Что тестировать:**

#### Вариант A: SCN как ASCII коды в OID (текущий подход)
```
OID: BASE_OID.1.67.79.49.49.49.49.49
SCN: CO11111 → ASCII: 67.79.49.49.49.49.49
```

#### Вариант B: SCN как одно число (если SCN только цифры)
```
OID: BASE_OID.1.11111
SCN: CO11111 → только цифры: 11111
```

#### Вариант C: SCN как строка в значении varbind
```
OID: BASE_OID.1
Value: "CO11111" (в значении, а не в OID)
```

#### Вариант D: SCN как часть community string
```
Community: "UTMC:CO11111"
OID: BASE_OID.1
```

#### Вариант E: SCN как отдельный varbind в SET запросе
```
Varbind 1: utcControlSiteID = "CO11111"
Varbind 2: utcControlFn = 0x01
```

#### Вариант F: SCN без преобразования (как есть)
```
OID: BASE_OID.1.C.O.1.1.1.1.1
(но это невозможно в SNMP, так как OID только числа)
```

**Команды для тестирования:**
```bash
# Вариант A (текущий - не работает)
./build/test_controller set 192.168.75.150 UTMC \
  1.3.6.1.4.1.13267.3.2.4.2.1.5.1.67.79.49.49.49.49.49 \
  4 "$(printf '\x01')"

# Вариант B (только цифры)
./build/test_controller set 192.168.75.150 UTMC \
  1.3.6.1.4.1.13267.3.2.4.2.1.5.1.11111 \
  4 "$(printf '\x01')"

# Вариант C (SCN в значении - нужно проверить MIB)
# Вариант D (SCN в community - маловероятно)
# Вариант E (SCN как отдельный varbind - нужно проверить MIB)
```

---

### 6. **Анализ MIB файла более детально**

**Что искать в MIB:**
- Структура таблицы `utcControlTable`
- Как именно передается `utcControlSiteID`
- Примеры использования из MIB комментариев
- Описание формата индексов таблицы

**Команды:**
```bash
# Поиск в MIB
grep -A 20 "utcControlSiteID\|utcControlTimeStamp\|INDEX" \
  docs/UTMC-UTMCFULLUTCTYPE2-MIB*.txt
```

---

### 7. **Сравнение с другими протоколами**

**Что искать:**
- Как передается SCN в других протоколах (STSIP, и т.д.)
- Есть ли различия в формате
- Примеры из других интеграций

---

## 🔍 Вопросы для исследования

### Вопрос 1: Формат OID с SCN
- Как именно передается SCN в OID?
- Нужно ли преобразование перед передачей?
- Есть ли примеры успешных запросов?

### Вопрос 2: Обязательность SCN
- Действительно ли SCN обязателен для SET операций?
- Может быть, контроллер работает и без SCN (обратная совместимость)?
- Есть ли разница в поведении с SCN и без SCN?

### Вопрос 3: Формат SCN
- Может быть, SCN нужно передавать не как ASCII коды, а как-то иначе?
- Есть ли преобразование "CO11111" → число?
- Может быть, только цифровая часть SCN используется?

### Вопрос 4: Табличная структура
- Как именно работает таблица `utcControlTable`?
- Нужно ли передавать timestamp и siteID как отдельные varbinds?
- Или они должны быть частью OID?

---

## 📝 Шаблон для сбора информации

```markdown
## Информация о формате SCN

### 1. Исходный код (Node.js)
- [ ] Найден файл spectr_utmc.js
- [ ] Найдена функция формирования OID
- [ ] Найдено использование SCN
- Примеры кода:
  ```javascript
  // Вставить примеры
  ```

### 2. Примеры реальных запросов
- [ ] Найдены логи успешных запросов
- [ ] Найдены примеры OID с SCN
- Примеры:
  ```
  OID: ...
  SCN: ...
  ```

### 3. Документация
- [ ] Найдена документация по формату SCN
- [ ] Найдены примеры использования
- Ссылки: ...

### 4. Тестирование
- [ ] Протестирован вариант A (ASCII коды) - не работает
- [ ] Протестирован вариант B (только цифры) - ?
- [ ] Протестирован вариант C (в значении) - ?
- Результаты: ...

### 5. MIB анализ
- [ ] Проанализирована структура таблицы
- [ ] Найдено описание формата индексов
- Выводы: ...
```

---

## 🎯 Приоритеты исследования

1. **Высокий приоритет:**
   - Исходный Node.js код (если доступен)
   - Примеры реальных успешных запросов
   - Документация контроллера

2. **Средний приоритет:**
   - Тестирование разных форматов
   - Анализ MIB файла
   - Сравнение с другими протоколами

3. **Низкий приоритет:**
   - Примеры других проектов
   - Форумы/сообщества

---

## 💡 Что делать прямо сейчас

1. **Проверить исходный Node.js код:**
   ```bash
   find . -name "*.js" -exec grep -l "utcControlFn\|siteId\|SCN" {} \;
   ```

2. **Проверить логи (если есть):**
   ```bash
   grep -i "snmp\|oid\|scn" /path/to/logs/*.log
   ```

3. **Связаться с производителем:**
   - Запросить документацию по формату SCN
   - Запросить примеры успешных запросов

4. **Протестировать альтернативные форматы:**
   - Вариант B (только цифры)
   - Вариант E (SCN как отдельный varbind)

---

## 📞 Контакты для запроса информации

- **Производитель контроллера:** Синтез
- **Техническая поддержка:** (если известен контакт)
- **Документация:** Веб-интерфейс контроллера, PDF файлы

---

**Цель:** Определить правильный формат передачи SCN для успешной работы SET операций с контроллером.
