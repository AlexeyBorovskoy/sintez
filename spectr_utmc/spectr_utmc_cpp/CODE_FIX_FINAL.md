# ИСПРАВЛЕНИЕ: Убрана передача SCN из OID

**Дата:** 2026-02-03  
**Основание:** Анализ реального работающего кода `spectr_utmc.js`

---

## 🎯 ПРОБЛЕМА

Документация указывала "+ SCN", но реальный работающий код показывает, что **SCN не используется в OID вообще**.

---

## ✅ РЕШЕНИЕ

Убрана передача SCN из OID. Теперь используются фиксированные OID, как в работающем коде.

---

## 📋 ВНЕСЕННЫЕ ИЗМЕНЕНИЯ

### 1. **setPhase()** - Установка фазы
**Было:**
```cpp
phaseVarbind.oid = buildOIDWithSCN(SNMPOID::UTC_CONTROL_FN);
```

**Стало:**
```cpp
phaseVarbind.oid = SNMPOID::UTC_CONTROL_FN;  // БЕЗ SCN
```

### 2. **setYF()** - Установка желтого мигания
**Было:**
```cpp
ffVarbind.oid = buildOIDWithSCN(SNMPOID::UTC_CONTROL_FF);
```

**Стало:**
```cpp
ffVarbind.oid = SNMPOID::UTC_CONTROL_FF;  // БЕЗ SCN
```

### 3. **setOS()** - Установка режима "Все выключено"
**Было:**
```cpp
loVarbind.oid = buildOIDWithSCN(SNMPOID::UTC_CONTROL_LO);
```

**Стало:**
```cpp
loVarbind.oid = SNMPOID::UTC_CONTROL_LO;  // БЕЗ SCN
```

### 4. **setLocal()** - Возврат в локальный режим
**Было:**
```cpp
loVarbind.oid = buildOIDWithSCN(SNMPOID::UTC_CONTROL_LO);
ffVarbind.oid = buildOIDWithSCN(SNMPOID::UTC_CONTROL_FF);
```

**Стало:**
```cpp
loVarbind.oid = SNMPOID::UTC_CONTROL_LO;  // БЕЗ SCN
ffVarbind.oid = SNMPOID::UTC_CONTROL_FF;  // БЕЗ SCN
```

### 5. **setStart()** - Запуск контроллера
**Было:**
```cpp
startVarbind.oid = buildOIDWithSCN(startOID);
```

**Стало:**
```cpp
startVarbind.oid = startOID;  // БЕЗ SCN
```

---

## 📊 ИСПОЛЬЗУЕМЫЕ OID (как в работающем коде)

| Операция | OID | Тип | Значение |
|----------|-----|-----|----------|
| Operation Mode | `1.3.6.1.4.1.13267.3.2.4.1` | INTEGER | 3 (UTC Control) |
| SetPhase | `1.3.6.1.4.1.13267.3.2.4.2.1.5` | OCTET_STR | Bit mask |
| SetAF | `1.3.6.1.4.1.13267.3.2.4.2.1.20` | INTEGER | 1 (включить) |
| SetOFF | `1.3.6.1.4.1.13267.3.2.4.2.1.11` | INTEGER | 1 (включить) |
| SetStart | `1.3.6.1.4.1.13267.3.2.4.2.1.5.5` | INTEGER | 1 |

**Все OID фиксированные, БЕЗ SCN и БЕЗ дополнительных индексов!**

---

## ✅ РЕЗУЛЬТАТЫ

- ✅ Код обновлен
- ✅ Компиляция успешна
- ✅ OID соответствуют работающему коду
- ✅ SCN убран из OID (как в оригинале)

---

## 🔍 ОБЪЯСНЕНИЕ РАСХОЖДЕНИЯ С ДОКУМЕНТАЦИЕЙ

**Вопрос:** Почему документация говорит "+ SCN", если код не использует SCN?

**Возможные объяснения:**

1. **Документация устарела** или относится к другой версии протокола
2. **SCN используется для других целей** (не в OID, а в другом месте)
3. **Контроллер поддерживает оба формата** (с SCN и без SCN)
4. **SCN нужен только для определенных GET операций**
5. **SCN используется в других протоколах** (не UG405)

**Факт:** Работающий код не использует SCN в OID, и операции работают корректно.

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Код исправлен
2. ⏳ Протестировать на реальном контроллере
3. ⏳ Визуально подтвердить работу
4. ⏳ Проверить все SET операции

---

## 🎯 ВЫВОД

**Код теперь соответствует работающему оригиналу!**

Все SET операции используют фиксированные OID без SCN, как в реальном работающем коде `spectr_utmc.js`.
